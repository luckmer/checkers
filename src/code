    // const MultipleOption = (detectValues, drop) => {
    //   const dualJump = detectValues.detectAttack;
    //   const dualJumpTest = detectValues.test;

    //   if (dualJump && dualJumpTest) {
    //     const optionA = dualJump.data;
    //     const optionB = dualJumpTest.data;

    //     const clearOptionA = optionA.filter((el) => el !== drop);
    //     const clearOptionB = optionB.filter((el) => el !== drop);

    //     if (!clearOptionA.length || !clearOptionB.length) {
    //       return false;
    //     }

    //     return true;
    //   }

    //   return false;
    // };
    // const controlMultipleOption = MultipleOption(detectValues, drop);

    // const controlSwitchPlayer = !detectValues?.jump
    //   ? detectValues?.jump
    //   : detectValues.data.filter((el) => el !== drop).length === 0 &&
    //     !test.data.length
    //   ? false
    //   : true;

    // const multipleJumpTester =
    //   controlMultipleOption && !controlSwitchPlayer
    //     ? controlMultipleOption
    //     : controlSwitchPlayer;

    // if (!multipleJumpTester) {
    // }