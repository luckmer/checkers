// import { TargetType } from "../../constants/helper";

// const targetDataType = (takeBlock, takePawn) => {
//   const FindTargetType = TargetType(takeBlock.type, 0);

//   let type = TargetType(takePawn.type, 0);

//   if (!type) type = TargetType(takePawn.type, 0);

//   return { type, FindTargetType };
// };

// export default targetDataType;
