// import { dataFinder } from "../../constants/helper";

// const finderDataType = (boardData, dropId, grabbedAsNumber) => {
//   const takeBlock = dataFinder(boardData, (finder) => finder._id === dropId);
//   const takePawn = dataFinder(
//     boardData,
//     (finder) => finder._id === grabbedAsNumber
//   );

//   return { takeBlock, takePawn };
// };

// export default finderDataType;
